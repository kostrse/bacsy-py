interface:
  display_name: "Release"
  short_description: "Prepare a bacsy release for maintainer approval"
  default_prompt: "Use $release to prepare the next bacsy release."
policy:
  allow_implicit_invocation: false
